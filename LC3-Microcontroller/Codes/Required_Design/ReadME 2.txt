To run and simulate the LC3-Microcontroller project, First compile the files.
 change the directory of the include file to the directory of the project (controller,decode,lc3_microcontroller,fetch,LC3_ideal) 

change the directory of the .txt which the memory reads in top_tb modul to the directory of the project
there is 3 examples which is given by mentor in the orientation session and the fourth one is an example made by us. all theses examples are in .txt files and the testbench will read them and put the instructions into memory. 

Top module is:
lc3_microcontroller

